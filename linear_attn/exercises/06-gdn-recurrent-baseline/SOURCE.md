# Source provenance

This exercise was developed and executed through the B300 synchronization repository:

- Repository: <https://github.com/Alizen-1009/MLFlow-B300-Sync>
- Branch: `lesson/gdn-recurrent-baseline`
- Initial implementation: `be88fb3`
- B300 result commit: `0f37852`
- Baseline report commit: `481bce0`

The Python implementations, raw `results.json`, and baseline report in this directory were copied byte-for-byte from that branch after the B300 SM103 correctness run.
